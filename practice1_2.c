#include <stdio.h>
int main()
{
    int r=3;
    float pi=3.14;
    printf("Area of the Circle is %f \n", pi*r*r);

    int height = 9;
    printf("The volume of the cylinder is %f ", pi*r*r*height);
    return 0;
}